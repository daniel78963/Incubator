<UserControl x:Class="Incubator.Desktop.Views.ConfiguracionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Incubator.Desktop.Views"
        mc:Ignorable="d" Height="450" Width="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>

    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Left" Width="300">
            <TextBlock Text="Prueba de Consumo Legacy (x86)" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Valor de entrada:" />
            <TextBox Text="{Binding InputValue, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Height="25" Padding="5,0"/>

            <Button Content="Ejecutar Cálculo Antiguo" 
                    Command="{Binding CalcularLegacyCommand}" 
                    IsEnabled="{Binding IsProcessing, Converter={StaticResource BoolToVisConverter}, ConverterParameter=Invert}"
                    Padding="10,5" Margin="0,0,0,20"/>

            <Border Background="#F0F0F0" Padding="15" CornerRadius="5">
                <TextBlock Text="{Binding ResultText}" FontSize="14" TextWrapping="Wrap" />
            </Border>

            <ProgressBar IsIndeterminate="True" Height="10" Margin="0,20,0,0"
                         Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisConverter}}" />
        </StackPanel>
    </Grid>
</UserControl>
