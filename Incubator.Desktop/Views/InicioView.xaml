<UserControl x:Class="Incubator.Desktop.Views.InicioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:Button Grid.Row="0" 
                   Content="Obtener Clientes" 
                   Command="{Binding CargarDatosCommand}" 
                   Icon="{ui:SymbolIcon ArrowDownload24}"
                   Appearance="Primary"
                   Margin="0,0,0,15"/>

        <ui:DataGrid Grid.Row="1" 
                     ItemsSource="{Binding Clients}" 
                     AutoGenerateColumns="True" 
                     IsReadOnly="True" />

        <Grid Grid.Row="1" 
              Background="#80000000" 
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}">

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ui:ProgressRing IsIndeterminate="True" Width="60" Height="60" Margin="0,0,0,15" />
                <ui:TextBlock Text="Conectando a BD..." FontTypography="Subtitle" Foreground="White" />
            </StackPanel>

        </Grid>
    </Grid>
</UserControl>