<Window x:Class="Incubator.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Incubator.Desktop"
        xmlns:viewmodels="clr-namespace:Incubator.Desktop.ViewModels"
        xmlns:views="clr-namespace:Incubator.Desktop.Views"
        mc:Ignorable="d"
        Title="{Binding TituloVentana}" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:InicioViewModel}">
            <views:InicioView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewmodels:ConfiguracionViewModel}">
            <views:ConfiguracionView />
        </DataTemplate>
    </Window.Resources>
    <Grid>
        
        <!--<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">

            <TextBlock Text="{Binding TituloVentana}" 
                       FontSize="20" 
                       FontWeight="Bold"
                       TextAlignment="Center"
                       Margin="0,0,0,10"/>

            <TextBlock Text="{Binding ContadorClicks, StringFormat='Clics: {0}'}" 
                       FontSize="16" 
                       TextAlignment="Center"
                       Margin="0,0,0,30"/>

            <Button Content="Saludar al ViewModel" 
                    Command="{Binding SaludarCommand}" 
                    Padding="10,5" 
                    Margin="0,0,0,10"
                    Width="200"/>

            <Button Content="Simular Carga Asíncrona" 
                    Command="{Binding CargarDatosCommand}" 
                    Padding="10,5" 
                    Width="200"/>

        </StackPanel>
        <StackPanel Margin="20" HorizontalAlignment="Center">

            <TextBlock Text="{Binding Mensaje}" Margin="0,0,0,20" />

            <TextBox x:Name="TxtNombre" Width="200" Margin="0,0,0,10" />

            <Button Content="Enviar Nombre al ViewModel"
            Command="{Binding SaludarUsuarioCommand}"
            CommandParameter="{Binding ElementName=TxtNombre, Path=Text}" 
            Width="200" Padding="10"/>

        </StackPanel>-->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Background="#EEE">
                <Button Content="Ir a Inicio" Command="{Binding NavegarAInicioCommand}" Margin="0,0,0,10"/>
                <Button Content="Ir a Configuración" Command="{Binding NavegarAConfiguracionCommand}"/>
            </StackPanel>

            <ContentControl Grid.Column="1" Content="{Binding VistaActual}" Margin="20" />
        </Grid>
    </Grid>
</Window>
